dist
.wrangler
.output
.vercel
.netlify
.vinxi
app.config.timestamp_*.js

# Environment
.env
.env*.local

# dependencies
/node_modules

# IDEs and editors
/.idea
.project
.classpath
*.launch
.settings/

# Temp
gitignore

# System Files
.DS_Store
Thumbs.db

# Harness (thesis evaluation tool — generated data)
harness/harness.db
harness/harness.db-journal
harness/harness.db-wal
harness/harness.db-shm
# Legacy runs dir from before runs were moved outside the repo for isolation.
# New runs land in <homedir>/.slop-harness/runs; this entry kept to ignore
# any stray files left over from earlier sessions.
harness/runs/
